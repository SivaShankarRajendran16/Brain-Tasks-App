version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 365998109313.dkr.ecr.us-east-1.amazonaws.com
  build:
    commands:
      - echo Build started on `date`
      - docker build -t brain-tasks-app:latest .
      - docker tag brain-tasks-app:latest 365998109313.dkr.ecr.us-east-1.amazonaws.com/brain-task-app:latest
  post_build:
    commands:
      - echo Pushing the Docker image...
      - docker push 365998109313.dkr.ecr.us-east-1.amazonaws.com/brain-task-app:latest
      - echo Build completed on `date`


